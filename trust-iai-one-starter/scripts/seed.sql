-- Phase 1 seed.
-- Per Operational Trust Surface rule: do NOT pre-seed fake demo data.
-- D1 begins empty. Real audit logs accumulate from real user reports only.
-- Real incidents are added through human-reviewed disclosure, never seeded.

-- (intentionally empty)
